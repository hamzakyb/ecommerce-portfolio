import PropTypes from "prop-types";
import "./Tabs.css";

const Tabs = () => {
  return null;
};

export default Tabs;

Tabs.propTypes = {
  singleProduct: PropTypes.object,
  setSingleProduct: PropTypes.func,
};